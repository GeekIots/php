### 流程图
```
graph TD
    a[登陆] --> b(搜索MySQL openID是否存在)
    b --> |存在| c(直接登陆) 
    c --> h1(完成)
    b --> |不存在| d(询问是否注册过)
    d --> |未注册| e(用QQ信息注册并登陆)
    e --> h2(完成)
    d --> |已注册| f(提示绑定QQ号)
    f --> |验证已注册账号| g(绑定QQ号并登陆)
    g --> h3(完成)
```